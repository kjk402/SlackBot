import com.hubspot.slack.client.SlackClient;

public class Forecasting {
    public static void main(String[] args) {
        SlackClient client = BasicRuntimeConfig.getClient();
        //PostAMessage.messageChannel("bat", client, "");
    }
}
